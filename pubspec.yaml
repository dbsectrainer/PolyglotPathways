name: polyglot_pathways
description: A multilingual language learning application for 50-day structured curriculum
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # Local Storage
  shared_preferences: ^2.2.2

  # Audio Player
  audioplayers: ^5.2.1

  # JSON handling
  json_annotation: ^4.8.1

  # Intl for date formatting
  intl: ^0.20.2

  # Path provider for file access
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  json_serializable: ^6.7.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/translations/
    - assets/lessons/

  # fonts:
    # - family: Poppins
    #   fonts:
    #     - asset: fonts/Poppins-Regular.ttf
    #     - asset: fonts/Poppins-Bold.ttf
    #       weight: 700
